# mcp-${API_NAME_DASH}

A standalone MCP server for **${API_DESCRIPTION}**.

Generated from a workflow-discovered API pattern. Drop into any MCP-compatible
client (Claude Desktop, Cursor, etc.) and the tool becomes available
immediately after a client restart.

## Install

```bash
git clone https://github.com/AIWander/mcp-${API_NAME_DASH}.git
cd mcp-${API_NAME_DASH}
cargo build --release
# Binary at: target/release/${API_NAME_DASH}
```

## Configuration

This server requires one environment variable:

| Variable | Purpose |
|---|---|
| `${CRED_ENV_VAR}` | Credential for the upstream API |

Set it before launching the binary, or in your MCP client's server config.

## Claude Desktop config

```json
{
  "mcpServers": {
    "${API_NAME_UNDERSCORE}": {
      "command": "/absolute/path/to/${API_NAME_DASH}",
      "env": {
        "${CRED_ENV_VAR}": "your-credential-here"
      }
    }
  }
}
```

## Tool surface

This server exposes one tool: `${API_NAME}` (description above). See
`./schema.json` for the full input schema, generated from the original
captured pattern.

## Provenance

- Generated by: `workflow:api_graduate`
- Source pattern stored: `${SOURCE_TIMESTAMP}`
- Original observation session: `${SOURCE_SESSION_ID}`
- Template repo: [AIWander/mcp-template-from-api](https://github.com/AIWander/mcp-template-from-api)

## License

MIT.
